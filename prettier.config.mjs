/** @type {import("prettier").Config} */
const config = {
  printWidth: 88,
  singleQuote: false,
  trailingComma: "all",
  bracketSpacing: true,
  plugins: ["prettier-plugin-tailwindcss"],
};

export default config;
